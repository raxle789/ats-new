BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[employment_status] ADD [alias] NVARCHAR(510);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
