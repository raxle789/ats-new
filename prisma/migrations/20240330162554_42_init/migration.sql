BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[education_levels] ALTER COLUMN [proint_id] INT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
