BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[candidates] ALTER COLUMN [gender] NVARCHAR(32) NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
